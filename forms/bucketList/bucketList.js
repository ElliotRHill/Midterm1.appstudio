let bucketList = [ ]
alert('Do you want to see your bucket list?')
if (alert == true)
    console.log(bucketList)
else
    let userBucketList = prompt('Enter an item into your bucket list')
    bucketList.push(userBucketList)
